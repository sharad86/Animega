<%-include("../partials/header")%>
<div class="row">
	<div class="col-md-4">
		
		<h1><%= user.firstName + " " + user.lastName %></h1>
		<div class="card">
		<img src="<%= user.image%>" class="card-img-top" alt="user profile image">
		<div class="card-body"><a href="mailto:<%= user.email%>"><%= user.email%></a></div>
	</div>
	Bio goes here!.
	</div>
	<div class="col-md-8">
    <h3><%= user.username %>'s WatchList</h3>
	<ul>
	<% animes.forEach(function(animes){%>
     <li><a href="/animes/<%=animes.id%>"><%= animes.name%></a></li>
    <%});%>
    </ul>
	</div>
</div>
<%-include("../partials/footer")%>